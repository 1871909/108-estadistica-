



#Normalidad -------------------------------------------

head (conjunto)

# Aplicar prueba de shapiro. test para la variable Altura 

shapiro.test(conjunto$Altura)

ks.test(conjunto$diametro), ´´pnorm´´, mean=mean(conujunto$diametro
                                                 sd=sd(conjunto$diametro))

hist(conjunto$diametro)